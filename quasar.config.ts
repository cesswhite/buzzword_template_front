export const quasar_config = {
    components: {
        defaults: {
            QBtn: {
                /* dense: true, */
                /* flat: true, */
            },
            QInput: {
                /* dense: true */
            }
        }
    }
}